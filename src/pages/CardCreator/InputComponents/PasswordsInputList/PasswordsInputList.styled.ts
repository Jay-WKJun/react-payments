import { styled } from '@/stitches.config';

export const StyledPasswordsContainer = styled('div', {
  display: 'flex',
});

export const StyledHiddenPassword = styled('span', {
  // @ts-ignore
  flexCenter: '',
  width: '15px',
  marginRight: '10px',
});
