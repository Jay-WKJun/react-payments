import { styled, css } from '@/stitches.config';

export const StyledSecurityCodeInputContainer = styled('div', {
  display: 'flex',
  alignItems: 'center',
  gap: '15px',
});

export const securityCodeInputStyle = css({
  width: '25px',
});
